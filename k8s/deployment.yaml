apiVersion: apps/v1
kind: Deployment
metadata:
  name: langfuse-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: langfuse-web
  template:
    metadata:
      labels:
        app: langfuse-web
    spec:
      containers:
        - name: langfuse-web
          image: langfuse/langfuse:3
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: langfuse-env
      imagePullSecrets:
        - name: acr-pull-secret
